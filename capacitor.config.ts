import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mateo.codeascention',
  appName: 'projectcodeascention',
  webDir: 'out'
};

export default config;
